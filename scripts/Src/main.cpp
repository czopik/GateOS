// NOTE: Legacy monolithic sketch entry point.
// This project uses src/app_main.cpp as the ONLY entry point.
//
// Keeping this file (disabled) helps preserve old code for reference,
// but it must not compile to avoid duplicate setup()/loop() symbols.

#if 0
#include <Arduino.h>
void setup() {}
void loop() {}
#endif
